package test.horstman_book_examples.first

import scala.math._

object Horstman_1 extends App{
  val temp = sqrt(3);
  val temp2 = pow(temp, temp);
  println(temp2);

  val temp3 = "hello" * 3;
  println(temp3);

  val line = "Hello"
  println(line.charAt(0));
  println(line.charAt(line.length-1))
}
